#include <stdio.h>
#define MAXLEN 1000

typedef struct {
    char username[MAXLEN];
    char password[MAXLEN];
} User;

// Parses /DB and creates a linked list of User structs
User* parse_db() {
    
    return;
}